version: '3.8'
services:
  backend:
    build: ./
    container_name: backend
    restart: always
    env_file:
      - ./.env
    ports:
      - 5000:5000
  frontend:
    build: ./client/react-frontend
    container_name: frontend
    restart: always
    ports:
      - 5173:5173